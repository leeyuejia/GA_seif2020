let teeth;
let money;

const start = ()=> {
    teeth = 16;
    money = 0;
    askForAction();
}

const showStatus = () => {
    alert("You have " + teeth + " and " + "$" + money + " left");
}

const askForAction = () => {
    showStatus();
    const choice = prompt('What do you want to do?', "use teeth / ");
    if (choice ===)
}

